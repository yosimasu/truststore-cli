# Story 2.6: Certificate Type Detection Enhancement

## Status
Approved

## Story
**As a** DevOps engineer,
**I want** the certificate chain service to intelligently detect certificate types before processing,
**so that** self-signed certificates are handled correctly without unnecessary external API calls.

## Acceptance Criteria

**Smart Detection Logic:**

1. Enhance existing certificate chain service with `detectCertificateType()` function
2. Self-signed detection criteria: subject equals issuer AND certificate validates against its own public key
3. CA-signed detection criteria: subject differs from issuer OR certificate cannot validate against its own public key
4. Function returns enum: `SELF_SIGNED`, `CA_SIGNED`, or `UNKNOWN`

**Integration with Existing Service:**
5. For `SELF_SIGNED` certificates: Return single-certificate chain immediately, skip CT log calls
6. For `CA_SIGNED` certificates: Use existing CT log completion logic from implemented Story 2.1
7. For `UNKNOWN` certificates: Default to CA-signed behavior with warning logged
8. Maintains backward compatibility with all existing certificate processing flows

**Quality Assurance:**
9. Unit tests cover edge cases: intermediate CAs, cross-signed certificates, malformed certificates
10. Performance testing shows detection adds minimal overhead (<10ms) to certificate processing
11. Integration tests verify correct behavior for both self-signed and CA-signed certificate workflows
12. Error handling for corrupted or invalid certificate data

## Tasks / Subtasks

- [ ] **Task 1: Create Certificate Type Enumeration** (AC: 4)
  - [ ] Define `CertificateType` enum in `internal/service/` with values: `SELF_SIGNED`, `CA_SIGNED`, `UNKNOWN`
  - [ ] Add string representation methods for debugging and logging
  - [ ] Include appropriate documentation for each certificate type

- [ ] **Task 2: Implement `detectCertificateType()` Function** (AC: 1, 2, 3, 4)
  - [ ] Add function to existing certificate chain service in `internal/service/chain.go`
  - [ ] Implement subject vs issuer comparison logic for self-signed detection
  - [ ] Implement public key signature validation for self-signed certificates
  - [ ] Add CA-signed detection logic using inverse conditions
  - [ ] Return `UNKNOWN` for edge cases and malformed certificates
  - [ ] Add comprehensive error handling for certificate validation failures

- [ ] **Task 3: Integrate Detection Logic with Chain Building** (AC: 5, 6, 7, 8)
  - [ ] Modify existing chain completion logic to call `detectCertificateType()` first
  - [ ] For `SELF_SIGNED`: Return immediate single-certificate chain, skip CT log calls
  - [ ] For `CA_SIGNED`: Use existing CT log completion logic from Story 2.1
  - [ ] For `UNKNOWN`: Default to CA-signed behavior with warning logged
  - [ ] Ensure backward compatibility with existing certificate processing workflows

- [ ] **Task 4: Comprehensive Unit Testing** (AC: 9, 12)
  - [ ] Create test certificates for edge cases: intermediate CAs, cross-signed certificates
  - [ ] Add tests for malformed certificate handling and error scenarios
  - [ ] Test self-signed detection accuracy with various certificate types
  - [ ] Test CA-signed detection accuracy with complex certificate chains
  - [ ] Validate error handling for corrupted certificate data

- [ ] **Task 5: Performance and Integration Testing** (AC: 10, 11)
  - [ ] Benchmark detection function to ensure <10ms overhead requirement
  - [ ] Integration tests verifying self-signed certificate workflows skip CT log calls
  - [ ] Integration tests verifying CA-signed certificate workflows use CT log completion
  - [ ] Regression tests ensuring existing certificate processing flows remain unchanged

## Dev Notes

### Previous Story Insights
[Source: Story 2.5 completion notes]
- Self-signed certificate detection implemented using `IsSelfSigned()` method in Certificate Chain Service
- Interactive confirmation system and audit logging patterns established for security-critical operations
- Certificate fingerprint calculation and display functionality available for debugging
- Existing service integration patterns maintain backward compatibility while adding new functionality

### Certificate Chain Service Architecture
[Source: docs/architecture/05-5-components.md#certificate-chain-service]
- **Component Location**: `internal/service/chain.go` - Certificate Chain Completion Service
- **Current Functionality**: Implements logic for building complete certificate chains using CT log infrastructure
- **Key Interfaces**: `BuildChain(certificate)` returns complete certificate chains
- **Dependencies**: `CT Log Client` for recursive certificate fetching, existing `IsSelfSigned()` method
- **Integration Point**: Enhanced with intelligent certificate type detection for conditional processing

### Technology Stack Requirements
[Source: docs/architecture/03-3-tech-stack.md]
- **Go Version**: 1.25.0 (managed via `asdf` and `.tool-versions` file)
- **No External Libraries**: Use Go's standard `crypto/x509` package for certificate operations
- **Linting Tool**: `golangci-lint` v1.59.1 for code quality enforcement
- **Pure Go**: No CGO usage to maintain maximum portability

### File Locations and Project Structure
[Source: docs/architecture/08-8-source-tree.md]
- **Primary Implementation**: `internal/service/chain.go` - Enhance existing Certificate Chain Service
- **Certificate Type Definition**: Add `CertificateType` enum to `internal/service/` package
- **Test Files**: `internal/service/chain_test.go` - Co-located unit tests
- **Test Data**: `internal/service/testdata/` - Store test certificates for various scenarios

### Detection Logic Specifications
[Source: Epic 2.6 AC#2, AC#3]
- **Self-signed Detection**: 
  - Primary: Subject equals Issuer (string comparison)
  - Secondary: Certificate validates against its own public key using `crypto/x509` validation
- **CA-signed Detection**: 
  - Primary: Subject differs from Issuer
  - Secondary: Certificate cannot validate against its own public key
- **Unknown Handling**: Return `UNKNOWN` for malformed certificates or validation failures

### Performance Requirements
[Source: Epic 2.6 AC#10]
- **Detection Overhead**: Must add less than 10ms to certificate processing
- **Optimization Strategy**: Cache detection results within the chain service if beneficial
- **Benchmarking**: Use Go's built-in `testing.B` for performance validation

### Integration Requirements
[Source: Epic 2.6 AC#5, AC#6, AC#7, AC#8]
- **Backward Compatibility**: All existing certificate processing flows must remain unchanged
- **CT Log Integration**: Leverage existing Story 2.1 CT log completion logic for CA-signed certificates
- **Self-signed Optimization**: Skip CT log calls entirely for self-signed certificates to improve performance
- **Error Handling**: Maintain existing error handling patterns using `fmt.Errorf` with `%w` verb

### Code Quality Standards
[Source: docs/architecture/11-11-coding-standards.md]
- **Error Handling**: Never discard errors, use `fmt.Errorf` with `%w` verb for wrapping
- **Interface Usage**: Maintain existing service interface patterns for dependency injection
- **Linting Requirements**: All code must pass `golangci-lint` before commit
- **No CGO**: Maintain pure Go implementation for maximum portability

## Testing

### Testing Standards
[Source: docs/architecture/12-12-test-strategy-and-standards.md]
- **Test File Location**: `internal/service/chain_test.go` - Co-located with implementation
- **Test Framework**: Go's built-in `testing` package - no external frameworks required
- **Coverage Goal**: >80% unit test coverage for new detection functionality
- **Test Data Management**: Store test certificates in `internal/service/testdata/` subdirectory

### Unit Testing Requirements
- **Certificate Type Detection**: Test detection accuracy for various certificate types
- **Edge Case Handling**: Test intermediate CAs, cross-signed certificates, malformed certificates
- **Performance Testing**: Benchmark detection function using `testing.B` to verify <10ms requirement
- **Error Handling**: Test behavior with corrupted or invalid certificate data
- **Integration Testing**: Verify detection integrates correctly with existing chain building logic

### Integration Testing Requirements
- **Self-signed Workflow**: Verify self-signed certificates skip CT log calls and return single-certificate chains
- **CA-signed Workflow**: Verify CA-signed certificates use existing CT log completion logic
- **Unknown Certificate Handling**: Verify unknown certificates default to CA-signed behavior with warnings
- **Backward Compatibility**: Regression tests ensuring existing functionality remains unchanged

### Test Data Requirements
- **Self-signed Certificates**: Various self-signed certificates for positive detection testing
- **CA-signed Certificates**: Certificate chains with intermediate CAs for CA-signed detection
- **Edge Cases**: Cross-signed certificates, expired certificates, malformed certificate data
- **Mock CT Log Responses**: Use `net/http/httptest` for mocking CT log API responses in integration tests

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-09-04 | 1.0 | Initial story draft created from Epic 2.6 requirements with comprehensive technical context | Bob (Scrum Master) |